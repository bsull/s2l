<% if Rails.env.production? %>
  <%= @current_account.customers.select('name').where('name ilike ?', '%'+params[:q]+'%').all.collect {|c| c.name}.join("\n") %>
<% else %>
  <%= @current_account.customers.select('name').where('name like ?', '%'+params[:q]+'%').all.collect {|c| c.name}.join("\n") %>
<% end %>